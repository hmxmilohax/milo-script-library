(define TempBlurInterface 
   (description "This handles the ftue squence to drive the mask changes to the blur") 
   (props 
      (ftue_step int 
         (default 0) 
         (help "integer driven by the steps in RBFTUESequenceCom"))))
