#ifdef HX_PC
#endif
#ifdef HX_DURANGO
(ms_config
   (title_id 831654665)
   (scid
      "28be3d3b-5edc-40ee-84cf-02a7e9428bfe"))
(dis_config
   #ifdef _SHIP
   (environment
      "prod")
   #else
   (environment
      "stage")
   #endif)
#endif
#ifdef HX_XBOX
(ms_config
   (title_id 1112934436))
(dis_config
   #ifdef _SHIP
   (environment
      "prod")
   #else
   (environment
      "stage")
   #endif)
#endif