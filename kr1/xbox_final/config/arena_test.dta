#macros.dta

(spew
   ; (CharAnims)
)

(rnd
   (panes_showing TRUE)
   (panes
      (arena_test (lines 2) (showing TRUE))
      (arena (lines 4) (showing TRUE))
   )
)
   

(arena
   #arena.dta
)

(charsys
   #charsys.dta
)

(db
   #db.dta
)

(quick-cheats
   (keyboard
      ("w" {camtest_begin})
      ("e" {camtest_end})
      ("d" {camtest_stage})
      ("s" {camtest_crowd})
      ("q" {camtest_l_char})
      ("r" {camtest_r_char})
      ("a" {camtest_l_corner})
      ("f" {camtest_r_corner})
      ("z" {camtest_crowdtest})
   )
)

(timer
   cycle ; needed for budget
   sync  ; needed for budget
   venue
   "(venue fx)"
   chars
)

##default.dta
