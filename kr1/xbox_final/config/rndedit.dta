; Configuration file for RndEdit application

#macros.dta

;(spew (loader))

(timer
   ; rndps2 timers
   flush
   gs_sync

   ; rndobj timers
   anim
   updateworldxfm
   draw
   sync
   cycle
)

(mem
   (track_stl TRUE)
   (enable_tracking TRUE)
)

(rnd
   (panes_showing TRUE)
   (panes
      (rate (showing FALSE))
   )
)

(rndedit
   (memtrack
      (details *Mesh mFacePacket mEdgePacket Vert)
      (details *Resource_ Bitmap_buf PsMat PsTex)
      (particle Particle ParticleSys)
      (keys *Key)
      (other *Rnd *Anim View Cam Light Environ)
      )
   )

##default.dta
