(define Interface 
   (description "A simple interface to expose abstract active/inactive state on an instance.") 
   (props 
      (active bool 
         (help "Should this instance be active?") 
         (set_on_instance TRUE))))
