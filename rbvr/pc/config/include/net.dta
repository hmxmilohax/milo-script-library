
#ifdef _SHIP

(default_environment live)

#else 

(default_environment dev)

#endif 

(default_server deltaDNA)

#ifdef HX_PC

(user_agent "HMX - PC")
(dev 
   (servers 
      (deltaDNA 
         (hostname "collect10584rckbn.deltadna.net") 
         (root "/collect/api/31030556401994587859963664014837/bulk/") 
         (port 443) 
         (use_ssl TRUE))))
(live 
   (servers 
      (deltaDNA 
         (hostname "collect10584rckbn.deltadna.net") 
         (root "/collect/api/31030561819471121663467457114837/bulk/") 
         (port 443) 
         (use_ssl TRUE))))

#endif 

