(define Interface 
   (description "Basic setup for a Horizontal List") 
   (props 
      (list_type symbol 
         (help "The section of the shell, drives coloring of the screen header.") 
         (allowed_values 
            (
               (icon "We want to use an icon!") 
               (color "Just a color please!") 
               (finish "If for some reason we need a third thing!"))) 
         (set_on_instance TRUE) 
         (default icon))))
