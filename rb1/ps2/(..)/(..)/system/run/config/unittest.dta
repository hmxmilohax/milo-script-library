; unit test config for system
#include macros.dta

; allocate just a little memory, for faster statup
(mem
   (heaps   
      (main  (size 10000000))
   )
)

; test data goes here

#merge default.dta
