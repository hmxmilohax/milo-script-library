; macros for sound effects

; Claps for star power mode
#define CLAP_DELAY_BEATS (0.9) ; play this fraction of a beat late

; Delay on excitement changes before switching
; to the new crowd audio loop
#define CROWD_AUDIO_DELAY_BEATS (1)