#include macros.dta
(rnd
   (title
      "UI Test"))
(namespaces
   (ui 5000 40000))
(ui
   (shared_rnd_files ui/shared.rnd)
   (create
      #include ../ui/start.dta
      {ui goto_screen start}))
#merge default.dta