(RndDir
   (types
      (count_in
         (editor
            (show script (script {$this show}))
            (start script (script {$this start}))
            (reset script (script {$this reset}))
         )
         (start {count_in.trig trigger})
         (reset {reset.trig trigger})
         (show {count_in.anim set_frame 10})
      )
   )
)