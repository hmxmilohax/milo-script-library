#include macros.dta
(mem (heaps (main (size 10000000))))
(system (language (supported (eng esl fre))))
(test_format_array
   (
      (planet planet_name)
      (earth_num 1616)
      (one 1)
      (residents "denizens")
      (num_people 5551212)
   )
)
#merge default_ugc.dta