#include macros.dta
(objects
   (UISlider
      (resource_file "../ui/slider.milo")
      (types
         (horizontal (vertical FALSE))
         (vertical (vertical TRUE))
      )
   )
   (UITextField
      (test FALSE)
      (superclasses UIComponent)
      (allowed_dirs PanelDir)
      (types (default (resource_file "../ui/text_field.milo")))
   )
   #merge ../m2/m2_objects.dta
)
#merge default.dta