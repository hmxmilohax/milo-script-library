#include macros.dta
(objects
   (UITextField
      (test FALSE)
      (superclasses UIComponent)
      (allowed_dirs PanelDir)
      (types
         (default
            (resource_file
               "../ui/text_field.milo"))))
   #merge ../m2/m2_objects.dta)
#merge default.dta