(default_hometown boston)
(default_money 100)
#define ONEOFF_HINT
((advance
      {{tour performer}
         add_seen_hint
         {{tour hint}
            get_type}}))
(band
   #include tour_band.dta)
(solo
   #include tour_solo.dta)
#ifndef _SHIP
(test_event
   (songs random random random)
   (reward_per_star 100))
#endif