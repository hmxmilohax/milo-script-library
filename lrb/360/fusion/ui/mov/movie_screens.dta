{new UIPanel
   mov_alien_sign_panel
   #ifdef HX_WII
   (file "mov_alien_sign_wii.milo")
   #else
   (file "mov_alien_sign.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_alien_sign_screen (panels mov_alien_sign_panel)}
{new UIPanel
   mov_auditions1_panel
   #ifdef HX_WII
   (file "mov_auditions1_wii.milo")
   #else
   (file "mov_auditions1.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_auditions1_screen (panels mov_auditions1_panel)}
{new UIPanel
   mov_auditions2_panel
   #ifdef HX_WII
   (file "mov_auditions2_wii.milo")
   #else
   (file "mov_auditions2.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_auditions2_screen (panels mov_auditions2_panel)}
{new UIPanel
   mov_auditions3_panel
   #ifdef HX_WII
   (file "mov_auditions3_wii.milo")
   #else
   (file "mov_auditions3.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_auditions3_screen (panels mov_auditions3_panel)}
{new UIPanel
   mov_castle_attack_panel
   #ifdef HX_WII
   (file "mov_castle_attack_wii.milo")
   #else
   (file "mov_castle_attack.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_castle_attack_screen (panels mov_castle_attack_panel)}
{new UIPanel
   mov_breakdown_panel
   #ifdef HX_WII
   (file "mov_breakdown_wii.milo")
   #else
   (file "mov_breakdown.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_breakdown_screen (panels mov_breakdown_panel)}
{new UIPanel
   mov_dinochase_panel
   #ifdef HX_WII
   (file "mov_dinochase_wii.milo")
   #else
   (file "mov_dinochase.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_dinochase_screen (panels mov_dinochase_panel)}
{new UIPanel
   mov_moonsetup_panel
   #ifdef HX_WII
   (file "mov_moonsetup_wii.milo")
   #else
   (file "mov_moonsetup.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_moonsetup_screen (panels mov_moonsetup_panel)}
{new UIPanel
   mov_demolition_panel
   #ifdef HX_WII
   (file "mov_demolition_wii.milo")
   #else
   (file "mov_demolition.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_demolition_screen (panels mov_demolition_panel)}
{new UIPanel
   mov_abyssclub_panel
   #ifdef HX_WII
   (file "mov_abyssclub_wii.milo")
   #else
   (file "mov_abyssclub.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_abyssclub_screen (panels mov_abyssclub_panel)}
{new UIPanel
   mov_octopus_panel
   #ifdef HX_WII
   (file "mov_octopus_wii.milo")
   #else
   (file "mov_octopus.milo")
   #endif
   (BUTTON_DOWN_MSG {$this move_on})
   (poll
      {if {TexMovie.tmov get played}
         {$this move_on}
      }
   )
   (move_on {ui goto_screen main_screen})
}
{new UIScreen mov_octopus_screen (panels mov_octopus_panel)}